const readline = require('readline-sync');
/**
 * @module PrimeFactorBl.js
 */
module.exports = {
    /**
    * @param {number} N - to comput its prime factors
    * @return {number Array} factors - all the prime factors of N
    */
    readArray: (row, column) => {
        console.log("Enter " + (row * column) + " elements");
        let twoDArr = new Array[2];

        for (let i = 0; i < row; i++) {
            for (let j = 0; j < column; j++) {
                twoDArr[i][j].p;
            }
        }
        return twoDArr;
    }
    
}